services:
  frontend:
    build: .
    command: yarn start
    ports:
      - "3000:3000"
    environment:
      - LUI_API_BASE_URL=${LUI_API_BASE_URL}
    extra_hosts:
      - "localhost:host-gateway"